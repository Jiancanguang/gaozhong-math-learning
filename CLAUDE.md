# 项目约定

## 沟通语言
- 与用户的所有沟通一律使用中文
- Git commit 消息使用中文
- PR 标题和描述使用中文
- 代码注释保持英文（行业惯例），但面向用户的说明用中文

## 更新进度板块
- 位置：`app/about/page.tsx` 的 `updateLogs` 数组
- 排序：最新的记录放在数组最前面（倒序），最早的放最后
- 大的功能变更（新功能上线、系统重构、模块新增等）需要同步添加一条更新记录
- 小的改动（样式微调、bug 修复、组件替换等）不需要更新
